#include <stdio.h>
#include <string.h>
#include <stdlib.h>
void change (int *a)
{
	(*a)++;
}

int main(void)
{
	// int a = 10123;
	// char str[80] = {};
	// char *xmp;
	// xmp = (char *)malloc(sizeof(char) * 5);
	// char *p = xmp;
	// // change(&a);'
	// for (int i = 0; a > 0; i++)
	// {

	// 	xmp[i] = (a % 10) + 48;
	// 	a = a / 10;
	// 	// xmp++;
	// }
	// a = a / 10;
	// xmp[3] = (a % 10) + 48;
	// xmp[4] = '\0';
	printf("%10%\n");
	return 0;
}
